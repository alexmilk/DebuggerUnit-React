import React from "react";
import ReactDOM from "react-dom";


export default class Layout extends React.Component {

    render() {
        return (
         <div></div>
    );
    }
    }

    ReactDOM.render(<Layout />, document.getElementById('topNavBar'));